---
phase: 01-rebrand
plan: 03
type: execute
depends_on: ["01-01", "01-02"]
files_modified: [README.md]
domain: docs
---

<objective>
Bring README.md up to the accurate, Desky-branded, multi-instance reality so the
public repo's front page is correct the moment Phase 1 lands.

Purpose: The repo is already public as `didi6135/Desky`, but README still says
"Claudify" AND describes the abandoned single-instance design (wrong service name
`claude-telegram.service`, single `~/.claudify/` dir). This plan fixes both.
Output: A README that matches what the installer actually does after 01-01/01-02 —
named multi-instance installs under `~/.desky-<name>/`, `desky-<name>.service`,
the `~/.local/bin/<name>` personal command — with working `didi6135/Desky` URLs.

**This is the base rebrand of the README only.** The deep docs rewrite (container
model from Phase 4, `docs/architecture.md`, `docs/troubleshooting.md`,
`docs/faq.md`, and the repo-wide zero-stale-`claudify` grep gate) stays in Phase 3.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/references/checkpoints.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rebrand/01-01-SUMMARY.md
@.planning/phases/01-rebrand/01-02-SUMMARY.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@README.md
@lib/layout.sh
@lib/service.sh
@lib/personal-cmd.sh

**Depends on 01-01 + 01-02:** by the time this runs, the code produces
`~/.desky-<name>/` + `desky-<name>.service`, and the install URLs point at
`didi6135/Desky`. The README must describe THAT state.

**Why a separate plan:** A doc rewrite with architecture corrections is a distinct
concern from the code/URL renames; isolating it keeps each plan focused and lets
this one get a human-verify pass on readability.
</context>

<accuracy_guardrails>
**Document only what ships. Do NOT overclaim.**
- **No container / "host-isolated" claims.** Container isolation is Phase 4 (not
  built; the mount-namespace approach even failed against AppArmor on 24.04 — see
  STATE.md blocker). Describe current isolation honestly: per-instance state +
  systemd Tier-1 hardening. Don't contradict the repo About, but don't promise
  containers in the README yet.
- **Doctor check count:** the codebase has an unresolved "28 vs 34" discrepancy
  (pinned for Phase 9). Read the real count from `doctor.sh` at execution, or write
  "a battery of health checks" without a hard number. Do NOT assert "28".
- **Verify paths against code, don't assume.** Confirm the partial-state file
  location, the per-instance directory tree, and the wrapper subcommands against
  `lib/layout.sh`, `lib/onboarding.sh`, `lib/personal-cmd.sh` before writing them.
- **Only touch README.md.** Link to `docs/*` (Phase 3 fixes their contents); do not
  rewrite them here.
</accuracy_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md to the Desky multi-instance reality</name>
  <files>README.md</files>
  <action>
Rewrite README.md so it is brand-accurate (Desky) and architecture-accurate
(multi-instance), correcting these specific staleness points found in the current
file:
- Title/tagline: "Claudify" → "Desky"; tagline reflects the positioning
  (self-hosted personal AI assistant on your own Linux server via Telegram).
- Install: one-liner → `https://raw.githubusercontent.com/didi6135/Desky/main/dist/install.sh`;
  document the default instance name (the operator's username via `whoami`) AND
  the `--name <instance>` flag for multiple instances. Keep the Ctrl-C/resume note
  but correct the partial-state path to the real per-instance location (verify in
  `lib/onboarding.sh`/`lib/layout.sh`).
- "After install": service is `desky-<name>.service` (NOT `claude-telegram.service`).
  Add the `~/.local/bin/<name>` personal command (`<name> doctor|update|status|logs|
  restart|start|stop|uninstall`) as the primary ergonomic interface; show the raw
  `systemctl --user … desky-<name>` equivalents as the fallback.
- "Everything lives here": replace the single `~/.claudify/` tree with the
  multi-instance `~/.desky-<name>/` tree (workspace/, channels/telegram/, mcps/,
  skills/, hooks/, data/, claude/, credentials.env) + mention the side-car
  `~/.desky-registry.json` and per-instance `desky.json` manifest. Match
  `lib/layout.sh` exactly.
- uninstall / doctor / update: update all URLs to `didi6135/Desky`; prefer the
  `<name> uninstall|doctor|update` wrapper form, with the curl form as fallback.
  Apply the doctor-count guardrail.
- Development table: keep accurate to current `STRUCTURE.md` (it's mostly fine;
  fix any `claudify` strings and the `~/.claudify*` references).
- Apply the <accuracy_guardrails>. Keep the License (TBD) section.
  </action>
  <verify>
1. `grep -niE 'claudify|claude-telegram' README.md` returns nothing.
2. `grep -nE 'didi6135/Desky' README.md` present; no `didi6135/Claudify` remains.
3. Every `~/.desky-<name>/…` path and the service name match `lib/layout.sh` / `lib/service.sh`.
4. No "container"/"host-isolated" capability claim beyond current systemd hardening.
5. All `docs/…` and `.planning/…` relative links still resolve.
  </verify>
  <done>README is Desky-branded, multi-instance-accurate, URLs point at didi6135/Desky, no stale single-instance or claude-telegram references, no overclaimed isolation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rewritten README.md (Desky brand + multi-instance architecture + correct install/doctor/update/uninstall URLs)</what-built>
  <how-to-verify>
    1. Open README.md (or view it on github.com/didi6135/Desky after push).
    2. Read the top fold: does the name + tagline land the "self-hosted AI assistant for businesses/freelancers" positioning?
    3. Check the Install + After-install sections: do the commands, the `<name>` wrapper, and the `~/.desky-<name>/` tree read as accurate and copy-pasteable?
    4. Confirm nothing overclaims container isolation.
  </how-to-verify>
  <resume-signal>Type "approved" to finish the plan, or describe edits to make.</resume-signal>
</task>

</tasks>

<verification>
Before declaring the plan complete:
- [ ] No `claudify` or `claude-telegram` strings in README.md
- [ ] All repo URLs are `didi6135/Desky` and resolve
- [ ] Paths/service/wrapper match the actual code (`lib/layout.sh`, `lib/service.sh`, `lib/personal-cmd.sh`)
- [ ] No isolation claim beyond shipped reality
- [ ] Human-verify checkpoint approved
</verification>

<success_criteria>
- README.md accurately represents Desky's shipped multi-instance behavior
- Public repo front page is brand-consistent and copy-pasteable
- Deep docs rewrite correctly left for Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/01-rebrand/01-03-SUMMARY.md`:

# Phase 1 Plan 03: README Refresh Summary

**[One-liner — README now accurate to Desky multi-instance]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Phase Readiness
Phase 1 complete. Phase 3 still owns the deep docs sync (container model,
architecture.md, troubleshooting.md, faq.md) + the repo-wide zero-stale-claudify
grep gate, which will also sweep CLAUDE.md / AGENTS.md.
</output>
