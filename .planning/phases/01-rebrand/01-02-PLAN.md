---
phase: 01-rebrand
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [install.sh, update.sh, lib/personal-cmd.sh, build.sh, lib/validate.sh, CHANGELOG.md, dist/install.sh]
domain: bash
---

<objective>
Switch the public identity from Claudify to Desky: rename the GitHub repository,
point every install/raw URL at the new repo, bump `SCRIPT_VERSION`, and finish the
`validate_instance_name` blocklist. After this plan the curl one-liner installs
"Desky" end to end.

Purpose: Completes Phase 1 — the outward-facing half of the rebrand that 01-01
deliberately left coupled to the repo name.
Output: Renamed `didi6135/Desky` repo, all URL values updated, version bumped,
blocklist hardened, `dist/install.sh` rebuilt, CHANGELOG entry.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/references/checkpoints.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rebrand/01-01-SUMMARY.md
@install.sh
@update.sh
@lib/personal-cmd.sh
@lib/validate.sh
@build.sh

**Depends on 01-01:** symbols/paths/service/markers already renamed; the only
`claudify` left in code is the `didi6135/Claudify` URL value (this plan) and
`Claudify-e4a` bead IDs (permanent).

**Constraining decisions:**
- Renaming the GitHub repo is outward-facing — confirm before executing (CLAUDE.md
  "things I never do without asking"). GitHub auto-redirects the old repo + raw
  URLs, so existing one-liners keep working during the transition.
- `dist/install.sh` is generated — rebuild via `build.sh`, never hand-edit.
- `SCRIPT_VERSION` bumps for user-visible change (CLAUDE.md rule 11). v1.0.0 is
  reserved for Phase 9 signoff — bump dev minor only.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Rename the public GitHub repository didi6135/Claudify → didi6135/Desky</action>
  <instructions>
This is the one outward-facing step. `gh` can do it, but it renames a public repo,
so confirm first.

Proposed commands (I can run them if `gh` is authenticated, or you run them):
  gh repo rename Desky -R didi6135/Claudify
  git remote set-url origin https://github.com/didi6135/Desky.git

GitHub keeps a redirect from the old name (`didi6135/Claudify` and its
`raw.githubusercontent.com` paths still resolve), so nothing breaks mid-transition.
  </instructions>
  <verification>`gh repo view didi6135/Desky` succeeds; `git remote -v` shows the Desky URL; `curl -fsSL -o /dev/null -w '%{http_code}' https://raw.githubusercontent.com/didi6135/Desky/main/dist/install.sh` returns 200.</verification>
  <resume-signal>Type "go" to have me run the rename, or "done" if you renamed it yourself.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Repoint URLs, bump version, finish blocklist</name>
  <files>install.sh, update.sh, lib/personal-cmd.sh, build.sh, lib/validate.sh</files>
  <action>
- Replace the URL value substring `didi6135/Claudify` → `didi6135/Desky` everywhere
  it appears in code: `install.sh` header, `update.sh` (curl/dist/uninstall URLs +
  `DIST_URL`), `lib/personal-cmd.sh` `DESKY_RAW_BASE` value, and the `build.sh`
  header heredoc (`Source:` + curl examples). This clears the last intentional
  `claudify` exception from 01-01.
- Bump `SCRIPT_VERSION` in `install.sh` from `0.1.0-dev` to `0.2.0-dev`.
- In `lib/validate.sh`, ensure the `validate_instance_name` blocklist rejects all
  of `claude`, `claudify`, AND `desky` (01-01 turned the old `claudify` entry into
  `desky`; re-add `claudify` so a migrating operator can't name an instance after
  either brand). Keep `claude`.
  </action>
  <verify>
1. `git grep -nE 'didi6135/Claudify' -- ':!.beads' ':!CHANGELOG.md'` returns nothing (URLs all point at Desky; only `.beads` dolt_database name + CHANGELOG history may retain "Claudify").
2. `grep '^SCRIPT_VERSION=' install.sh` shows `0.2.0-dev`.
3. `validate_instance_name claude`, `validate_instance_name claudify`, `validate_instance_name desky` each return non-zero (blocked).
4. `bash -n` passes on every touched file.
  </verify>
  <done>All code URLs point at didi6135/Desky; version bumped; blocklist blocks claude/claudify/desky.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild dist, verify install URL, lint + CHANGELOG</name>
  <files>dist/install.sh, CHANGELOG.md</files>
  <action>
Run `bash build.sh` to regenerate `dist/install.sh` with the new URLs + version.
Run `shellcheck` on touched files. Add a `### Changed` entry under `##
[Unreleased]` in `CHANGELOG.md`: GitHub repo renamed to Desky, install URLs
updated, `SCRIPT_VERSION` → 0.2.0-dev, instance-name blocklist hardened (do not
alter historical entries). Re-run `bash test.sh` (or Station11 round-trip) to
confirm nothing regressed.
  </action>
  <verify>
1. `bash build.sh` succeeds; `grep -E 'didi6135' dist/install.sh` shows only `didi6135/Desky`.
2. `shellcheck` clean (or justified disables).
3. `curl -fsSL https://raw.githubusercontent.com/didi6135/Desky/main/dist/install.sh | head -5` (or HTTP 200 check) resolves to the Desky installer header.
4. Test suite green (local or Station11).
  </verify>
  <done>dist rebuilt with Desky URLs + new version; install URL resolves; lint clean; tests green; CHANGELOG updated.</done>
</task>

</tasks>

<verification>
Before declaring Phase 1 complete:
- [ ] `didi6135/Desky` repo exists; `git remote` points at it; old name redirects
- [ ] No `didi6135/Claudify` in code (only `.beads` dolt_database + CHANGELOG history)
- [ ] `SCRIPT_VERSION` = 0.2.0-dev; blocklist blocks claude/claudify/desky
- [ ] `bash build.sh` regenerated a consistent `dist/install.sh`
- [ ] Raw install URL returns 200 and serves the Desky installer
- [ ] `bash -n` + `shellcheck` pass; test suite green
- [ ] CHANGELOG `[Unreleased]` documents the repo rename + version bump
</verification>

<success_criteria>
- All tasks completed
- GitHub repo is didi6135/Desky and the curl one-liner installs Desky end to end
- No functional Claudify branding remains anywhere in the code path
- Phase 1 (Rebrand) complete — code, paths, service, registry, and public URLs all say Desky
</success_criteria>

<output>
After completion, create `.planning/phases/01-rebrand/01-02-SUMMARY.md`:

# Phase 1 Plan 02: Public Identity Summary

**[One-liner — repo renamed, URLs live, version bumped]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Phase Readiness
Phase 1 complete. Next: Phase 2 (Migration) — build `lib/migrate.sh` to migrate
existing installs (both pre-3.4.5 `~/.claudify/` and 3.4.5+ `~/.claudify-*/`) to
the Desky layout this phase introduced. Carry forward the migration note from
01-01-SUMMARY (dir, service, manifest filename + `.claudify_version` key, PATH
marker, persona markers all changed).
</output>
