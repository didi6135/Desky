# Task 3.6.2 — Filesystem isolation (`ProtectHome=tmpfs` + `BindPaths`)

**Status:** ⏳ pending
**Estimated effort:** ~45 min
**Depends on:** 3.6.1 (Tier-1 already in place), 3.4.5 (flat
per-instance layout from ADR 0006), 3.5 (paths stable)
**Blocks:** 3.6.3 (syscall filter is the next layer)
**Research:** [`.planning/research/security.md`](../../research/security.md) Part C §2
**Authority:** [ADR 0006 — Multi-client isolation](../../decisions/0006-multi-client-isolation.md)

## Goal

After this task, the bot runs in a **private mount namespace** where
only its own instance directory is visible. Other instances'
folders, `~/.ssh`, `~/.bashrc`, and the rest of `$HOME` are not
"read-protected" — they **don't exist** in the bot's filesystem
view at all.

This is the **single biggest security win** in the Phase 3.6
umbrella, and it's the actually-secure foundation for multi-client
deployments.

> **Note on the original plan:** This task was originally specified
> as `ProtectHome=read-only + ReadWritePaths=...`. That plan
> restricted writes only — reads of other instances' data still
> succeeded because all instances run as the same Linux user.
> [ADR 0006](../../decisions/0006-multi-client-isolation.md) supersedes that
> approach with mount-namespace isolation. See ADR 0006 for the
> full rationale; this spec captures the implementation.

## Why

Today, a compromised conversation or buggy skill has full write
access to the operator's entire home directory. With `bypassPermissions`
in effect (no per-tool prompts), there is nothing between the LLM's
decision to write a file and the file being written. Filesystem
write-restriction at the systemd layer is the right place to bound
this.

See `.planning/research/security.md` Part C §2 for rationale.

## Scope

### Files to modify

- `lib/service.sh` — `write_service` heredoc gains `ProtectHome` +
  `ReadWritePaths`
- (potentially) `lib/engines/claude-code.sh::_npm_prefix_setup` —
  if we move PATH wiring out of `~/.bashrc` (see "decisions")
- `CHANGELOG.md` — `### Security` entry

### Files to create

- none

## The block

Adds to the `[Service]` section, after the Tier-1 directives from 3.6.1:

```ini
# === Mount-namespace isolation ===
# $HOME starts as an empty tmpfs in this unit's mount namespace;
# only the explicitly bound paths are visible. Other instances'
# folders (~/.claudify-<other-name>/), ~/.ssh, ~/.bashrc, and
# everything else under $HOME literally don't exist in the bot's
# filesystem view. Per ADR 0006.
ProtectHome=tmpfs
BindPaths=%h/.claudify-<INSTANCE_NAME>
BindReadOnlyPaths=%h/.npm-global %h/.bun

# Per-instance Claude state lives inside the per-instance dir,
# not in user-wide ~/.claude. CLAUDE_CONFIG_DIR redirects everything.
Environment=CLAUDE_CONFIG_DIR=%h/.claudify-<INSTANCE_NAME>/claude
```

`<INSTANCE_NAME>` is interpolated by `write_service` (e.g.
`%h/.claudify-client-a` for the client-a instance).

### What's bound and why

| Path | Mode | Why |
|---|---|---|
| `%h/.claudify-<name>` | read-write | This instance's everything: state, manifest, OAuth, workspace, channels, MCPs, skills, hooks, data, claude config |
| `%h/.npm-global` | read-only | Where the `claude` binary lives. Bot needs to *run* claude, not modify the binary. |
| `%h/.bun` | read-only | Bun runtime. Same reason. |

### What's invisible to the bot

Inside the bot's mount namespace, `$HOME` looks like:

```
$HOME/                              ← empty tmpfs
├── .claudify-<this-instance>/      ← read-write
├── .npm-global/                    ← read-only
└── .bun/                           ← read-only
```

Everything else is gone:

- `~/.claudify-<other-instance>/` — other clients' data
- `~/.ssh/` — SSH keys, known_hosts
- `~/.bashrc`, `~/.profile`, `~/.zshrc`
- `~/.gnupg/`, `~/.aws/`, `~/.config/`
- Operator's personal files
- **Anything else under `$HOME`**

A prompt-injected `find ~/ -name '*.env' -exec cat {} \;` returns
nothing useful — those paths don't exist in the bot's view.

### Why `CLAUDE_CONFIG_DIR`?

Claude Code reads `~/.claude/` (settings, plugins cache) and
`~/.claude.json` (project trust state) by default. Both are
user-wide. For multi-client isolation, every instance needs its
own copy. Anthropic supports this via `CLAUDE_CONFIG_DIR`;
pointing it at `%h/.claudify-<name>/claude/` puts that state inside
the instance dir (which is bound) rather than in `~/.claude/`
(which is NOT bound, so wouldn't be visible anyway).

`engine_seed_state` in `lib/engines/claude-code.sh` is updated by
3.4.5 to write into `${CLAUDIFY_INSTANCE_DIR}/claude/` instead of
`~/.claude/`. This task confirms the systemd `Environment=` line is
correctly set.

### What about `~/.bashrc`?

Today's `_npm_prefix_setup` appends a `PATH` export line to
`~/.bashrc`:

```bash
echo "export PATH=\"$NPM_PREFIX/bin:\$PATH\"" >> "$rc"
```

**That's at install time, not at runtime.** The bot itself never
writes to `.bashrc` — only `install.sh` does, and `install.sh` runs
as the operator's interactive shell, NOT under the systemd unit.
So `ProtectHome=tmpfs` doesn't affect this code path.

## Decisions

### `ProtectHome=tmpfs` vs `=read-only` vs `=true`

| Option | What it does | Choice |
|---|---|---|
| `ProtectHome=true` | Hides $HOME entirely (empty bind, no readback) — same effect as tmpfs but no writable workspace for tmp files | ❌ |
| `ProtectHome=read-only` | Bot can read all of $HOME; can write only to `ReadWritePaths` | ❌ Read leak across instances; superseded by ADR 0006 |
| **`ProtectHome=tmpfs`** | $HOME replaced with empty tmpfs; only `BindPaths`/`BindReadOnlyPaths` are visible | ✅ Per ADR 0006 |

### Why not `ProtectSystem=strict`?

`ProtectSystem=strict` makes `/usr`, `/boot`, `/etc`, and other
system dirs read-only. Tempting, BUT: the npm-global symlinks for
`claude` go through `/usr/local/bin/claude` on some setups. If
`/usr` is unwritable and we ever try a global claude reinstall via
the bot, it'd fail. Tier-1's `ProtectKernelTunables` +
`ProtectControlGroups` + `ProtectClock` + `ProtectHostname` cover
most of `ProtectSystem`'s value without the foot-gun.

We could revisit `ProtectSystem=full` (which excludes `/etc`) in
Phase 5 if a hardening guide identifies it as low-risk for our
shape.

### Why `BindPaths` not `ReadWritePaths`?

`ReadWritePaths` only works as an exception to a less-restrictive
`ProtectHome=` mode. With `ProtectHome=tmpfs`, the listed paths
must be explicitly *bound* in (not just made writable in an
otherwise-readable home). That's `BindPaths` (read-write) and
`BindReadOnlyPaths` (read-only).

## Steps

1. Edit `lib/service.sh::write_service` — add the
   `ProtectHome=tmpfs` + `BindPaths=` + `BindReadOnlyPaths=` block
   after the Tier-1 block (3.6.1). Interpolate `$INSTANCE_NAME` for
   the BindPath. Add the `Environment=CLAUDE_CONFIG_DIR=...` line
   if not already set by 3.4.5.
2. **Sanity-check no installer code path writes outside the
   bound paths at install time.** `grep -rn 'echo.*>>.*\$HOME\|>> *~\|echo.*>>.*%h' lib/`.
   Install runs outside the unit, so this is informational only —
   but anything the *bot* writes at runtime must be inside
   `~/.claudify-<name>` (covered by `BindPaths`) or `~/.npm-global`
   / `~/.bun` (read-only, can't write).
3. `bash build.sh` — `dist/install.sh` rebuilds.
4. `bash -n` clean.
5. **Station11 round-trip — must include the namespace verification:**
   - Clean install
   - Service starts cleanly
   - Bot replies to a real Telegram message
   - **Verify the isolation actually works** — from outside the unit:
     ```bash
     pid=$(systemctl --user show claudify-default -p MainPID --value)
     sudo nsenter -t $pid -m -- ls -la $HOME
     ```
     Expected: only `.claudify-default/`, `.npm-global/`, `.bun/`
     visible. NOT `.ssh/`, NOT `.bashrc`, NOT any other home contents.
   - **Verify writes outside the bind paths fail** — from inside the unit:
     ```bash
     sudo nsenter -t $pid -m -- /bin/bash -c 'echo X > ~/.testfile' 2>&1
     # Expected: "No such file or directory" or "Read-only file system"
     ```
   - `bash ~/test/doctor.sh` all checks green
   - `systemd-analyze --user security` ≤ 3.0
6. CHANGELOG: `### Security — Bot service runs in a private mount namespace. ProtectHome=tmpfs + BindPaths=%h/.claudify-<name> + BindReadOnlyPaths=%h/.npm-global %h/.bun. Other instances' folders, ~/.ssh, ~/.bashrc, and the rest of $HOME do not exist in the bot's filesystem view. Per ADR 0006 — multi-client isolation. systemd-analyze score: 5.0 → ~3.0.`

## Acceptance criteria

- [ ] `ProtectHome=tmpfs` + `BindPaths=%h/.claudify-<name>` +
      `BindReadOnlyPaths=%h/.npm-global %h/.bun` present in the
      unit text emitted by `write_service`
- [ ] `Environment=CLAUDE_CONFIG_DIR=%h/.claudify-<name>/claude`
      present (set by 3.4.5; verify here)
- [ ] Station11 install completes; service active; doctor green
- [ ] Bot still replies to Telegram messages
- [ ] **Namespace isolation verification**: `nsenter` into the
      service's mount namespace and confirm only the bound paths
      are visible under `$HOME`. Other instances' folders, `~/.ssh`,
      `~/.bashrc` are absent.
- [ ] **Write-block verification**: writing to `~/.testfile` from
      inside the namespace fails with "No such file or directory"
      or equivalent
- [ ] **Multi-instance test (after 3.4.5 multi-instance lands)**:
      install two instances; from inside Client A's namespace,
      `~/.claudify-client-b/` is invisible
- [ ] `systemd-analyze --user security` ≤ 3.0
- [ ] CHANGELOG `### Security` entry under Unreleased

## Test plan

- **Local:** grep for any code path that writes outside the four paths
- **Local:** `bash build.sh && bash -n dist/install.sh`
- **Station11:** clean install + service-up + reply test + doctor
- **Station11:** explicit "try to write to ~/.testfile" verification
- **Station11:** `systemd-analyze` score check

## Out of scope

- Address families / syscall filter (3.6.3)
- Removing the `~/.bashrc` PATH append from install (works fine,
  install runs outside the unit)
- Hardening of skill processes individually (Phase 5)

## Notes / risks

- **Biggest risk: a path the bot needs to write that's not bound.**
  Mitigation: Station11 round-trip with full reply test catches
  this immediately (the bot will fail loudly, not silently). If we
  hit this in production, adding a path to `BindPaths` is a
  one-line fix that re-runs.
- **Future engines may have different write needs.** A Gemini
  adapter might write to `~/.config/gemini/`. We'd add another
  `BindPaths` entry. Could be moved to a per-engine
  `engine_systemd_extra` function in the future — out of scope here.
- **MCP subprocesses inherit this restriction.** Future
  `claudify-memory` MCP (Phase 4.0a) writes to
  `~/.claudify-<name>/data/` — already covered by the
  `BindPaths=%h/.claudify-<name>` line. Future skills write to their
  own data dirs — same coverage.
- **`%h` is systemd's expansion for $HOME** — works for user-mode
  units. Don't write `$HOME` literally; systemd doesn't expand it.
- **`BindPaths=` requires the source path to exist at unit start.**
  Both `~/.claudify-<name>` and `~/.npm-global` / `~/.bun` must
  exist before the unit starts. This is fine — install creates
  them all before enabling the unit.
- **`nsenter -m` requires sudo**, but only for *verification*. The
  install + runtime never need sudo for the namespace itself.
- **Single-instance users get the same isolation.** Even with one
  instance (`default`), the bot can't read `~/.ssh` or `~/.bashrc`.
  The defense isn't only for multi-client — it benefits everyone.
