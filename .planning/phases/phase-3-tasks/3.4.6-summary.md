# Task 3.4.6 — Personal command wrapper — Summary

**Status:** ✅ DONE 2026-05-20 (code + Station11 round-trip)
**Spec:** [3.4.6-personal-cmd.md](3.4.6-personal-cmd.md)
**Effort:** ~50 min (estimated ~45)
**Unblocks:** 3.4.8 (docs sync references the personal cmd)

## What landed

A wrapper at `~/.local/bin/<NAME>` that dispatches per-instance
subcommands. After install the operator runs `<name> doctor` instead
of `bash uninstall.sh --name <name>` — short, ergonomic per-instance
control surface (docs/architecture.md §3d).

Per ADR 0006 the wrapper is engine-agnostic: nothing inside it
references `claude` or any engine adapter; it only deals with
systemctl + Claudify entrypoints.

## Files created

| Path | Lines | Role |
|---|---|---|
| [lib/personal-cmd.sh](../../../lib/personal-cmd.sh) | 159 | 4 functions: install / uninstall / ensure_path / clean_path + the `_personal_cmd_write_wrapper` heredoc generator |
| [tests/bash/personal-cmd.bats](../../../tests/bash/personal-cmd.bats) | ~110 | 15 tests covering wrapper gen + `--help` + PATH idempotency + uninstall removal + clean_path correctness |
| [.planning/phases/phase-3-tasks/3.4.6-summary.md](3.4.6-summary.md) | (this file) | Phase doc summary |

## Files modified

| Path | Change |
|---|---|
| [install.sh](../../../install.sh) | Source `lib/personal-cmd.sh` after `manifest.sh`. main() calls `step "Install personal command"` + `personal_cmd_install "$INSTANCE_NAME"` after `manifest_set_channel`. |
| [build.sh](../../../build.sh) | `MODULES=` array adds `personal-cmd.sh` after `manifest.sh` (matches install.sh source order). |
| [lib/service.sh](../../../lib/service.sh) | `final_summary` swaps long `systemctl --user …` one-liners for `<INSTANCE_NAME> status / logs / doctor / --help`. Adds "open a new terminal or source ~/.bashrc" hint. |
| [uninstall.sh](../../../uninstall.sh) | Per-instance: removes `~/.local/bin/<name>` (step 3, before deleting the instance dir). Once the registry empties, calls an inline `_pcmd_clean_path_rc` that removes the `# Claudify PATH —` marker block from `~/.bashrc` / `~/.zshrc`. Header docstring updated to list both. |
| [lib/README.md](../../../lib/README.md) | Adds `personal-cmd.sh` row to the modules table. |
| [CHANGELOG.md](../../../CHANGELOG.md) | New Unreleased Added entry at the top. |
| [.planning/phases/phase-3-tasks/README.md](README.md) | Status board: 3.4.6 → ✅ done. |
| [.planning/phases/phase-3-tasks/3.4.6-personal-cmd.md](3.4.6-personal-cmd.md) | Header updated to DONE + pointer to summary. |
| [dist/install.sh](../../../dist/install.sh) | Rebuilt — now 2231 lines (was 1857). |

## Files deleted

None.

## Design choices

| Decision | Why |
|---|---|
| **Wrapper uses `bash <(curl …)` for doctor/update/uninstall** | Matches today's `update.sh` model — the operator's command always pulls today's entrypoint from origin, so a stale wrapper doesn't lock them onto an old `doctor`/`uninstall`. CLAUDE.md's "no `bash <(curl …)` without explaining" rule is about *us* fetching code mid-session; here the operator chose to run `<name> update` themselves. |
| **systemctl/journalctl subcommands stay local** | `status / logs / start / stop / restart` need no network — direct `systemctl --user … claudify-<name>` keeps them instant and offline-safe. |
| **Marker-tagged PATH line (`# Claudify PATH —`)** | One unambiguous string makes uninstall's awk removal reliable, immune to operator-added PATH lines, and survives `personal_cmd_ensure_path` running twice. |
| **`personal_cmd_ensure_path` updates both `~/.bashrc` AND `~/.zshrc` when present** | Operator's daily shell isn't ours to assume. Bash + Zsh cover ~99% of Ubuntu/Debian operators; `sh` / `dash` users handle PATH manually (documented in the spec's "Notes / risks"). |
| **`personal_cmd_clean_path` only fires when the registry empties** | Matches the existing registry-file-cleanup pattern in uninstall.sh: shared state survives partial uninstalls, only the *last* removal wipes shared state. |
| **Wrapper file regenerated on every install (overwrite, not patch)** | Updates ship a new template (e.g., a new subcommand) without diffing logic — last install always wins, and the operator can't end up with a half-old wrapper that lacks subcommands the spec promises. |
| **Idempotency baked into every public function** | `install` overwrites cleanly; `uninstall` handles already-gone files; `ensure_path` greps-then-appends; `clean_path` no-ops without the marker. Matches CLAUDE.md rule 8. |
| **No `~/.local/bin/<name>` doctor fallback for offline installs** | If the operator has no network, `<name> doctor` will fail — but so will everything else Claudify needs (Telegram API, raw.githubusercontent for update, the bot's polling). Building offline-doctor branching is yak-shaving for a network-required product. |

## Smoke results (local — Windows / Git Bash)

| Check | Result |
|---|---|
| `bash -n` on every touched `.sh` | ✓ clean (lib/personal-cmd.sh, install.sh, build.sh, uninstall.sh, lib/service.sh, dist/install.sh) |
| `bash build.sh` | ✓ rebuilt `dist/install.sh` (2231 lines, +374 from 3.4.5.2's 1857) |
| `bash -n dist/install.sh` | ✓ clean |
| `bash test.sh` | ✓ ts canary 2/0; bash bats skip (not installed locally — same as 3.4.5/3.4.5.1) |
| Manual: `personal_cmd_install test-x` | ✓ wrapper executable, `--help` mentions test-x, unknown subcommand exits 2 |
| Manual: `personal_cmd_ensure_path` x2 | ✓ exactly one marker after two runs, file without trailing newline gets one before append |
| Manual: `personal_cmd_clean_path` | ✓ marker + export line removed, surrounding aliases preserved |
| Manual: `personal_cmd_uninstall` x2 | ✓ wrapper removed, second call exits 0 (idempotent) |

## Station11 round-trip — ✅ DONE 2026-05-20

Ran end-to-end against the real Ubuntu 24.04 user-mode systemd box.
First `~/test/autoinstall.sh` had to be brought into the multi-instance
era (the on-disk copy from 2026-04-21 still spoke single-instance) —
new version added a `BOT_TOKEN` collision check (3.4.5+),
`INSTANCE_NAME` / `INSTALL_SRC` env overrides, Tier-1 directive
validation, and a 7th step that asserts the 3.4.6 wrapper +
`--help` mention + PATH marker + `status` works. Backup of the
pre-3.4.6 copy is at `~/test/autoinstall.sh.pre-3.4.6.bak`.

Sequence run (after pushing 87c807a to `origin/main`):

```bash
# 1. Remove the only existing instance (client-a) with main's
#    uninstall.sh — confirms the 3.4.6-aware uninstall path runs
#    on real systemd, including "personal command already gone"
#    (client-a predated 3.4.6 so no wrapper existed).
bash <(curl -fsSL https://raw.githubusercontent.com/didi6135/Claudify/main/uninstall.sh) \
  --name client-a --yes

# 2. Run the patched autoinstall — installs as 'default' from main.
bash ~/test/autoinstall.sh
```

Results:

| Check | Result |
|---|---|
| install.sh `━━━ Install personal command ━━━` step fires after manifest writes | ✓ |
| `~/.local/bin/default` written, chmod 755, contains correct dispatch table | ✓ |
| `# Claudify PATH —` marker + `export PATH="$HOME/.local/bin:$PATH"` in `~/.bashrc` | ✓ |
| Runtime PATH detection: warned "not yet in PATH for this shell" when running from a non-rc'd SSH session | ✓ |
| Fresh `bash -lc` (re-sources `.bashrc`) resolves `default` via PATH | ✓ |
| `default --help` lists all 8 subcommands and mentions `default` 9 times | ✓ |
| `default status` → real `systemctl --user status claudify-default`, shows Active=running, Tasks 23/200, Memory 205.8M/1G — Tier-1 hardening enforced by kernel | ✓ |
| `default doctor` → `bash <(curl …)` fetches latest doctor.sh, runs **34/34 checks green** incl. bot polling (409 expected), Bun MCP subprocess, per-instance unit paths, Tier-1 directives, manifest validity | ✓ |
| Registry has `"personal_cmd": "default"` for instance `default` | ✓ |
| Bot reachable on Telegram (@Claudify_test_bot) | ✓ |

The local smoke covered everything except the real-systemctl bits;
this round-trip closed that gap. Risk-wise: doctor reports 34/34
(vs. 35/35 in the 3.4.5 summary — one check went away between
3.4.5 and now, unrelated to 3.4.6).

## Notes for next tasks

- **3.4.7 migration:** when migrating a pre-3.4.5 `~/.claudify/` to
  `~/.claudify-default/`, call `personal_cmd_install default` at the
  tail of the migration so post-migration installs get the wrapper
  retroactively (otherwise the operator still has to relearn the long
  flags they thought they'd escaped).
- **3.4.8 docs sync:** README/architecture.md should show `default doctor`
  / `default update` in the operator-facing examples, with a footnote
  that `<name> doctor` works for any registered instance.
- **Future polish (deferred per spec):** tab-completion for the wrapper,
  a `claudify list` multi-instance dispatcher, and per-update wrapper
  refresh (today's "regenerated on next install/update" is adequate).
