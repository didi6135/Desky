# Task 3.4.2 — Split `lib/steps.sh` — Summary

**Status:** ✅ DONE 2026-04-30
**Spec:** [3.4.2-split-steps.md](3.4.2-split-steps.md)
**Effort:** ~1 hr (as estimated)
**Unblocks:** 3.4.3 (engine extraction has clean module boundaries)

## What landed

Pure refactor. `lib/steps.sh` (615 lines, 13 public functions) split
into 5 focused modules. Zero behavior change — verified via Station11
round-trip with `--preserve-state --non-interactive`.

### Files created (5)

| Module | Lines | What it owns |
|---|---|---|
| `lib/onboarding.sh` | 129 | `intro`, `guide_botfather`, `guide_userinfobot`, `collect_inputs` (split into `_collect_inputs_preserved` + `_collect_inputs_fresh` to stay under 50 lines/fn) |
| `lib/claude.sh` (TEMP) | 180 | constants `NPM_PREFIX`, `CLAUDIFY_ROOT`, `CLAUDIFY_WORKSPACE`, `CLAUDIFY_TELEGRAM`, `CREDS_FILE`; functions `setup_npm_prefix`, `install_claude`, `install_telegram_plugin`, `seed_claude_state` (helpers `_seed_claude_json` + `_seed_settings_json`), `claude_is_authed` |
| `lib/configs.sh` | 202 | `write_configs` (helpers `_write_bot_env` + `_write_access_json`), `seed_persona` (with data-only helper `_starter_persona_doc`) |
| `lib/service.sh` | 118 | `write_service`, `start_service`, `final_summary` |
| `lib/oauth.sh` | 92 | `oauth_setup` (helpers `_run_setup_token` + `_persist_oauth_token`) |

### Files modified

| Path | Change |
|---|---|
| `install.sh` | source order updated: `ui → args → prompts → validate → preflight → onboarding → claude → configs → service → oauth → main()`; comment explains why each module sources where it does |
| `build.sh` | `MODULES=` array reflects the new 10-file order |
| `lib/README.md` | function table replaced `steps.sh` row with one row per new module; "When to split" section rewritten with the 3.4.2 split as the canonical example |
| `dist/install.sh` | rebuilt — 1158 lines (was 1044). Diff is purely structural (file boundary comments + new helper boilerplate); no logic change |
| `CHANGELOG.md` | Unreleased gained a `### Changed` entry describing the split + Station11 verification |

### Files deleted

- `lib/steps.sh` — gone

## File / function size compliance

```
ui.sh          49      args.sh        67      validate.sh     8
prompts.sh     85      preflight.sh  160      onboarding.sh 129
claude.sh     180      configs.sh    202      service.sh    118
oauth.sh       92
```

All ≤ 300 lines (largest: `configs.sh` @ 202).

Function-size scan (`awk` over `^name() {` … `^}$`): one function over
50 lines — `_starter_persona_doc` (80 lines) in `configs.sh`. It is a
single `cat <<'PERSONA'` heredoc with no branches, no locals, no
side-effects beyond stdout. Treated as data delivery, not logic. The
file calls this out in a comment above the function. Acceptable
because the `configs.sh` file is still well under the 300-line file
limit and the helper is structurally trivial.

Helper-extraction summary (purely to stay under 50 lines/fn for
non-data functions):

| Original | Was | Now |
|---|---|---|
| `collect_inputs` | 56 | orchestrator (~10) + `_collect_inputs_preserved` + `_collect_inputs_fresh` |
| `seed_claude_state` | 57 | orchestrator (~14) + `_seed_claude_json` + `_seed_settings_json` |
| `write_configs` | 57 | orchestrator (~7) + `_write_bot_env` + `_write_access_json` |
| `oauth_setup` | 70 | orchestrator (~32) + `_run_setup_token` + `_persist_oauth_token` |

## Smoke results

| Check | Result |
|---|---|
| `bash -n` on every touched `.sh` | ✓ clean |
| File-size scan | ✓ all ≤ 300 (largest 202) |
| Function-size scan | ✓ all ≤ 50 except documented data-only helper |
| `bash build.sh` | ✓ rebuilt `dist/install.sh` (1158 lines) |
| `bash -n dist/install.sh` | ✓ clean |
| `bash install.sh --version` / `--help` | ✓ unchanged |
| `bash test.sh` | ✓ ts canary 2 pass / 0 fail; bash suite warn-skipped (bats absent locally) |

## Station11 round-trip

Server already had a healthy install. Verified the new modular
structure preserves it.

```text
scp -P 43532 dist/install.sh david@77.42.21.117:/tmp/install-3.4.2.sh
ssh ... 'bash /tmp/install-3.4.2.sh --preserve-state --non-interactive'
ssh ... 'bash ~/test/doctor.sh' → All 28 checks passed.
ssh ... 'systemctl --user is-active claude-telegram.service' → active
ssh ... 'rm -f /tmp/install-3.4.2.sh'   # cleanup
```

Install log on Station11: every step green
(preflight → config reuse → claude already installed → seed → plugin
already installed → configs preserved → service unit written +
enabled → persona preserved → OAuth preserved → service running).

## Notes for next tasks

- **3.4.3 (engine extraction)** can now grab `lib/claude.sh` cleanly:
  the Claude-Code-specific functions (`setup_npm_prefix`,
  `install_claude`, `install_telegram_plugin`, `seed_claude_state`,
  `claude_is_authed`) move into `lib/engines/claude-code.sh`. The
  Claudify-layout constants (`CLAUDIFY_ROOT`, `CLAUDIFY_WORKSPACE`,
  `CLAUDIFY_TELEGRAM`, `CREDS_FILE`) are layout-not-engine and need a
  permanent home — likely a new `lib/layout.sh` or top of
  `install.sh` itself.
- **3.4.5 (multi-instance)** still owns the rename of
  `claude-telegram.service` → `claudify-<instance>.service` and the
  migration logic to rename existing units on upgrade.
- **Test coverage:** with bats not yet installed locally, the bash
  suite is only warn-skipped. 3.4.4 introduces real `.bats` tests for
  entrypoints — pair with a one-time `apt install bats` in the dev
  setup notes when that lands.

## Not committed

Working tree shows: 1 deleted (`lib/steps.sh`) + 5 new lib files +
modified `install.sh`, `build.sh`, `lib/README.md`, `CHANGELOG.md`,
`dist/install.sh`, `.planning/phases/phase-3-tasks/3.4.2-split-steps.md`,
`.planning/phases/phase-3-tasks/3.4.2-summary.md`. Ready to stage when
the owner gives the go.
