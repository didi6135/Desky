# Task 3.4.8 — Docs sync after the refactor

**Status:** ⏳ pending
**Estimated effort:** ~30 min
**Depends on:** 3.4.6, 3.4.7 (everything that affects user-facing surfaces)
**Blocks:** 3.5

## Goal

After the architectural refactor lands, every user-facing doc reflects
the new model: `--name`, personal command, multi-instance, manifest.
README is the front door — it can't lie about how the tool works.

## Why

`CLAUDE.md` rule 2: "every new feature gets full docs in the same
commit." 3.4.1 → 3.4.7 each updated CHANGELOG, but README, the
in-doc examples, and PROJECT.md need a final pass to be coherent.

## Scope

### Files to modify

- `README.md` — update install / update / uninstall / doctor examples to show `--name` and the personal command UX
- `docs/architecture.md` — flip status markers from "this will exist" to "this exists" in the migration map
- `docs/troubleshooting.md` — update commands to match new patterns
- `docs/faq.md` — add 2-3 entries: *"how do I run multiple bots?"*, *"my old install still works, what changed?"*, *"can I rename an instance?"*
- `.planning/PROJECT.md` — Status block reflects the refactor landed
- `.planning/ROADMAP.md` — Phase 3 block updated
- `.planning/phases/phase-3-lifecycle.md` — overview matches reality
- `.planning/phases/phase-3-tasks/README.md` — status board flipped to ✅ for 3.4.x
- `CHANGELOG.md` — collapse the 3.4.x scattered entries into one cohesive `[0.2.0-dev]` section heading at the top of Unreleased

### Files to create

- none

### Files to delete

- none

## Steps

1. **README.md** rewrite:
   - "Install" — note default instance name, mention `--name` for multi-instance
   - "Update" — show personal-cmd UX: `default update` instead of `bash <(curl …)/update.sh)`
   - "Uninstall" — show `default uninstall` and the requirement of `--name` or `--all`
   - New short section "Multiple bots on one server" with one-paragraph explanation + 2-line example
   - Diagnose section updated to `default doctor`
2. **docs/architecture.md** § 3f migration map: flip "Action" column entries from future-tense to "Done in 3.4.x"
3. **docs/troubleshooting.md**: update every command line to use the personal command if applicable, and `systemctl --user … claudify-default` instead of `claude-telegram`
4. **docs/faq.md**: 3 new entries for multi-instance and the rename
5. **PROJECT.md** Status block: flip the relevant ⏳ entries to ✅
6. **ROADMAP.md** Phase 3 block: same
7. **phase-3-lifecycle.md**: overview reflects reality; mention each task closed in order
8. **phase-3-tasks/README.md** status board: ✅ for 3.4.1 through 3.4.7
9. **CHANGELOG.md**: organize the 3.4.x entries under one cohesive Unreleased heading. Possibly bump version: `## [0.2.0-dev]` (because 3.4 changes service unit name — that's a breaking change for anyone watching the unit by name)
10. Commit message: *"docs sync: README + architecture + planning current after Phase 3.4 refactor."*
11. **No code changes** — pure docs.

## Acceptance criteria

- [ ] README.md examples all use the new commands
- [ ] No README mention of `claude-telegram.service` (the old unit name)
- [ ] No README mention of `~/.claudify/credentials.env` paths (use `~/.claudify/instances/<name>/credentials.env`)
- [ ] docs/architecture.md migration map status flipped to "done"
- [ ] PROJECT.md status block reflects 3.4 done
- [ ] ROADMAP Phase 3 block: 3.4.1-3.4.8 all ✅
- [ ] phase-3-tasks/README.md status board reflects done state
- [ ] CHANGELOG has cohesive `[0.2.0-dev]` (or similar) section covering all 3.4 work
- [ ] No code changes in this commit

## Test plan

- Read README.md top to bottom — does it teach a new user the right commands?
- Run a Station11 update; copy the post-install summary lines and confirm they match the README's claimed UX.
- (Optional) Have a friend / Claude Code session in another window read the docs and try to install — see if anything is still confusing.

## Out of scope

- Backup/restore docs (3.5 task includes those)
- Security section (3.6 task adds it)
- Architecture doc deeper edits — only status markers and the migration table

## Notes / risks

- Easy to miss one mention of the old service name buried somewhere. Use `grep -rn "claude-telegram" .` to catch them all (excluding `legacy/` if any old refs survive in markdown for context).
- The version bump (`0.2.0-dev`) is a judgment call. Default to bumping when we change a publicly-observable interface (service name, command structure). Document the reasoning in CHANGELOG.
- This task lands LAST in Phase 3.4 — its job is to leave the docs cleanly aligned with the code. If something feels misaligned during this task, fix the code, not the doc, then re-do the doc.
