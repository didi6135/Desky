# Task 3.4.1 — Repo skeleton — Summary

**Status:** ✅ DONE 2026-04-28
**Spec:** [3.4.1-skeleton.md](3.4.1-skeleton.md)
**Effort:** ~30 min (as estimated)
**Unblocks:** 3.4.2, 3.4.3, 3.4.4

## What landed

Empty scaffolding so subsequent Phase 3.4 tasks drop files into the
right places without fighting structure decisions. No behavior change
to the live install path.

### Files created (12)

| Path | Purpose |
|---|---|
| `lib/engines/README.md` | engine-adapter contract (6 functions); empty until 3.4.3 |
| `src/package.json` | `name=claudify-src`, `type=module`, dev deps `@types/bun`+`typescript` |
| `src/tsconfig.json` | strict, ES2022, ESM, bun types, `noEmit` |
| `src/lib/README.md` | rules for future TS modules (ESM, ≤300/file, ≤50/fn) |
| `tests/README.md` | overview of the two suites + how to run |
| `tests/bash/README.md` | bats convention + run instructions |
| `tests/bash/canary.bats` | 2 tests (1==1, bash present) |
| `tests/ts/README.md` | bun test convention + run instructions |
| `tests/ts/canary.test.ts` | 2 tests (1===1, Bun runtime reachable) |
| `test.sh` | repo-root entry; flags `--bash`/`--ts`/`--strict`; warn-skips missing runners |

### Files modified (5)

| Path | Change |
|---|---|
| `.gitignore` | added `bun.lockb`, `src/dist/` (kept under "Bun + TS build output") |
| `README.md` | Development table gained `lib/engines/`, `src/`, `tests/`, `test.sh` rows; added a `bash test.sh` snippet |
| `lib/README.md` | function table gained an `engines/` row pointing at the 6-function contract |
| `CHANGELOG.md` | Unreleased: new "Repo skeleton for Phase 3.4" Added entry + new Removed section for `templates/` |
| `.planning/phases/phase-3-tasks/3.4.1-skeleton.md` | status flipped to ✅ DONE; acceptance criteria all checked |

### Files deleted (3)

| Path | Reason |
|---|---|
| `templates/access.json` | unused — install renders inline via heredocs in `lib/steps.sh` |
| `templates/claude-telegram.service` | unused — same |
| `templates/README.md` | folder became empty |

`templates/` directory is gone from git. Confirmed via grep that no
live `.sh` or `.json` referenced these files.

## Smoke results

| Check | Result |
|---|---|
| `bash -n test.sh` | ✓ syntax clean |
| `bash test.sh` | bash suite warn-skipped (bats not installed locally); ts suite: **2 pass / 0 fail**; exit 0 |
| `bash build.sh` | rebuilt `dist/install.sh` (1044 lines); only diff is the build-timestamp comment line — no behavior change |
| `bash -n dist/install.sh` | ✓ syntax clean |

Station11 round-trip: not required — this task is structural, no
install behavior changed.

## Acceptance criteria (all met)

- [x] `lib/engines/`, `src/`, `tests/bash/`, `tests/ts/` exist with README stubs
- [x] `src/tsconfig.json` + `src/package.json` valid
- [x] `tests/bash/canary.bats` runs green (warn-skipped locally — bats absent; will run in CI/Station11)
- [x] `tests/ts/canary.test.ts` runs green via `cd src && bun test ../tests/ts/`
- [x] `bash test.sh` runs both suites with appropriate skip messages
- [x] `templates/` gone from git
- [x] `.gitignore` updated; `dist/install.sh` still tracked
- [x] `bash build.sh` clean (timestamp-only diff)
- [x] CHANGELOG Unreleased updated

## Notes for next tasks

- **3.4.2** can now drop manifest helpers somewhere obvious — likely
  `lib/manifest.sh` (bash) or `src/lib/manifest.ts` (TS, when 3.5
  introduces real modules).
- **3.4.3** drops `lib/engines/claude-code.sh`; the README in that
  folder already documents the 6-function contract it must implement.
- **3.4.4** can land its first `.bats` integration test alongside
  `canary.bats` without touching scaffolding.
- **Test runner gap on dev machine:** bats isn't installed on this
  Windows box. `test.sh --strict` is intended for CI; locally we run
  without `--strict` and accept the warn-skip. If we want bats locally
  on Windows, options are WSL or a Docker-based runner — defer until
  3.4.4 actually has bash tests to run.

## Not committed

Per `CLAUDE.md` I don't auto-commit. Working tree shows 5 modified +
3 deleted + 4 new untracked paths, ready to stage when the owner
gives the go.
